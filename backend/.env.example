GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# Only needed when the mcp-server is not reachable at localhost:8001
# (e.g. overridden by docker-compose to http://mcp-server:8001)
MCP_SERVER_URL=http://localhost:8001
